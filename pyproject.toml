[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "robonana"
version = "0.1.0"
description = "A shared-FLUX.2 RoboTwin world-action model with a training-only DINOv3 flow target."
requires-python = ">=3.10"
dependencies = [
  "torch>=2.6",
  "einops>=0.8",
  "safetensors>=0.4.5",
]

[project.optional-dependencies]
dev = ["pytest>=8"]
train = [
  "accelerate>=1.10",
  "diffusers>=0.38",
  "h5py>=3.10",
  "imageio>=2.34",
  "imageio-ffmpeg>=0.5",
  "Pillow>=10",
  "timm>=1.0.20",
  "wandb>=0.27",
]
preprocess = [
  "diffusers>=0.38",
  "h5py>=3.10",
  "Pillow>=10",
  "transformers>=4.57",
  "timm>=1.0.20",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
